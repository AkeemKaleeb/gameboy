/**************************************************************************************************
* File Name         : io.rs
* Description       : Handles i/o operations including user input from keypad or gameboard
**************************************************************************************************/

use crate::memory::Memory;

pub struct IO {
    // IO state
}

impl IO {
    pub fn new() -> Self {
        IO {
            // Initialize IO
        }
    }

    pub fn step(&mut self, memory: &mut Memory) {
        // Handle IO operations
    }
}